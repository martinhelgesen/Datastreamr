Namespace Entities.CustomerStream
    Public Class CustomerStreamAggregate
        'Skriv dine egene methoder for å hente data.. 
        'Validering
        Private Sub ValidateCreateEntity(o As Entities.CustomerStream.CustomerStream)
        End Sub
        Private Sub ValidateUpdateEntity(o As Entities.CustomerStream.CustomerStream)
        End Sub
        Private Sub ValidateDeleteEntity(o As Entities.CustomerStream.CustomerStream)
        End Sub
    End Class
End Namespace
